# AI情报局 · 操作日志

> 本仓库用于沉淀「AI情报局」项目：一个输入 1-3 个 AI 产品/品牌/公司（自然语言提问或填名字），生成可视化竞品分析报告的技能/产品。
> 所有设计共识、TODO、变更都记录在此，保持与代码同步。

---

## 一、项目简介
- **产品名**：AI情报局
- **定位**：输入 1-3 个 AI 产品/品牌/公司 → 生成可视化竞品分析报告。
- **范围**：仅 AI 相关领域（大模型/对话、AI 绘画视频、AI 办公效率、AI 营销、AI 编程、AI 搜索、AI 公司/基建）。
- **阶段策略**：MVP = skill + workflow，不上 agent。后续阶段再产品化为网站、再评估 agent。

---

## 二、已达成共识（设计决策）
1. **范围锁定 AI**：MVP 只做 AI 相关；扩展到非 AI（SaaS/设计工具/其他行业）不是简单配置切换，需单独重构，不在当前设计假装轻松。
2. **输入双形态**：自然语言提问（"分析一下 Cursor 的竞品" / "Kimi 比 豆包好在哪"）或填 1-3 个名字，两种都收。
3. **双模式意图解析**：
   - **对比模式**：用户点名 N（1-3）个 → 只做这几个正面对比，不拉外部玩家；预置清单不触发。
   - **竞品分析模式**：用户只给 1 个主体 → 先深析该主体，再对比 **固定 5 个**竞品（预置清单作种子 + 搜索扩充）。
4. **流程（AB 合并版，只确认 1 次）**：① 解析（LLM 抽出对象/意图/维度/深度） → ② 一次轻确认（分析计划卡） → ③ 生成（查证→写报告→渲染）。不学 Skill A 的三次确认。
5. **计划卡精简**：去掉来源标注；"分析对象"改为"从输入解析出 N 个"；对比模式不显示额外竞品。计划卡里的候选竞品先用**预置清单**给初稿，用户确认后再**实搜扩充**最终名单。
6. **维度 7 个不重合**：市场与定位 / 核心功能与AI能力 / 用户体验 / 商业模式与定价 / 增长与运营 / 技术与生态 / 风险与短板。（已消掉"产品定位⊂市场""AI能力⊂核心功能"的重叠）
7. **加深深度 = 同维度更深，不补维度，人不二次选**。三档：快 / 标准 / 深（深=同维度更多源+交叉验证+更长）。
8. **报告 6 模块**：
   ① 一句话结论 + 核心发现（卡片）
   ② 竞争格局（市场地图 market map + 热力）
   ③ 关键维度对比（雷达图 + 热力矩阵）+ **优缺点对比卡**（模块③下方小卡片）
   ④ **差异化机会点**（仅当意图 = 找机会 / 规划产品 / 选型时显示；内容为竞争空白/可跟进领域）
   ⑤ 趋势与建议（Must / Should / Could 优先级看板）
   ⑥ 来源与口径（来源卡片 + 未查证项 + 评分口径说明）
9. **预置清单**：仅"竞品分析模式"的种子，**不限制搜索范围、不进对比模式**；是静态配置，**不是 RAG**。
10. **证据纪律**：每条结论标来源；无来源标「未查证」；评分必写口径；结论分「事实发现 / 分析判断 / 行动建议」三类。
11. **工具通用化**：不写 WorkBuddy/Codex 专有工具名，统一用：联网搜索、网页抓取、搜索图片、GitHub 获取、Reddit 获取、LLM 推理与撰写、自研渲染层。
12. **可视化自研**：市场地图 / 热力 / 雷达 / 优先级看板 / 来源卡片；重写 Skill A 的丑模板，前端重做。
13. **借鉴而非照搬**：只借 Skill A「先确认再行动」原则，其余不被其束缚。

---

## 三、未来 TODO（非 MVP）
- **RAG**：建 AI 产品档案向量知识库，查询时检索注入（替代/增强静态预置清单）。
- **评测集**：黄金 query + 期望质量报告，用于测与迭代报告质量。
- **数据源接入**：GitHub（易，公开 API）、Hacker News（易，公开 API）、Reddit（中，需 OAuth/限流）、社媒（难，登录墙，best-effort）。
- **定时刷新**：先做「定时重跑固定查询」的 workflow（非 agent）。
- **多 agent 并行调研**：市场/定价/体验/技术 各派一个 agent。
- **智能客服 agent（可选）**：解答产品使用/报告含义，与主线无关。
- **网站产品化（阶段 2）**：后端跑 skill 逻辑（LLM+检索）+ 前端重做可视化页，实现"输入→生成页面"上线。

---

## 四、变更记录
- **2026-08-22**：初始化仓库；落定 MVP 设计共识（双模式/7 维度/6 模块/计划卡/证据纪律）；编写 `ai-intel-bureau` skill v0.1（SKILL.md + references + 改进版渲染脚本 `build_report.py`）。
